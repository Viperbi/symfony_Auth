{% extends 'base.html.twig' %} {% block title %}Poduct{% endblock %} {% block
body %} {% if is_granted('PUBLIC_ACCESS') %}
<h1>Poduct</h1>

<table class="table">
	<tbody>
		<tr>
			<th>Id</th>
			<td>{{ poduct.id }}</td>
		</tr>
		<tr>
			<th>Name</th>
			<td>{{ poduct.name }}</td>
		</tr>
		<tr>
			<th>Description</th>
			<td>{{ poduct.description }}</td>
		</tr>
		<tr>
			<th>Prix</th>
			<td>{{ poduct.prix }}</td>
		</tr>
	</tbody>
</table>

<a href="{{ path('app_poduct_index') }}">back to list</a>
{% endif %} {% if is_granted('ROLE_ADMIN') %}

<a href="{{ path('app_poduct_edit', { id: poduct.id }) }}">edit</a>

{{ include("poduct/_delete_form.html.twig") }}
{% endif %} {% endblock %}
